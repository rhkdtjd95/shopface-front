import React from 'react';
import OccupationListContainer from '../../containers/occupation/OccupationListContainer';

const OccupationPage = ({ match }) => {
  return (
    <div>
      <OccupationListContainer />
    </div>
  );
};

export default OccupationPage;
